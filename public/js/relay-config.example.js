// Example of the generated relay config. The real file (relay-config.js) is
// written by scripts/setup-relay.sh with this deployment's Pages URL and is
// gitignored. Without it the login page simply skips the relay bounce.
window.PANEL_RELAY_PAGE = 'https://YOU.github.io/YOUR-RELAY-REPO/';
